#! /usr/bin/env python

import logging
import sys
from os.path import join, dirname, realpath


lib = join(dirname(dirname(realpath(__file__))), "lib/python")
if lib:
    sys.path.insert(0, lib)


from aliyun_sms_alert import main
main()

# vim: ts=4 sw=4 sts=4 et:
